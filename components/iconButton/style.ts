import { StyleSheet } from "react-native";
import { dark } from "../../utility/colors";

export const _styles = StyleSheet.create({
  iconButton: { backgroundColor: dark.button, borderRadius: 5 },
});
